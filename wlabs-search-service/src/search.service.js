/*
 * Uses fetch to talk to the walmart api
 * it caches results in memory
 * 
 */
export default class SearchService {
    constructor(){}
    find(query){
        if (!query) return '';
        return '';
    }
}
